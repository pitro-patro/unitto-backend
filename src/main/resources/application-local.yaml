server:
  port: 8080

spring:
  # Redis
  redis:
    host: 127.0.0.1
    port: 6379
  # MySQL
  mysql:
    driver_class_name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/unitto?characterEncoding=utf8
    user_name: pitro-patro
    password: pitro-patro
  # Thymeleaf
  thymeleaf:
    cache: false
    check-template-location: true
    prefix: classpath:/templates/
    mode: HTML5
    suffix: .html
    order: 0

service:
  lottery:
    timeout: 120
    max_try: 100